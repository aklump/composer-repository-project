s="${BASH_SOURCE[0]}";[[ "$s" ]] || s="${(%):-%N}";while [ -h "$s" ];do d="$(cd -P "$(dirname "$s")" && pwd)";s="$(readlink "$s")";[[ $s != /* ]] && s="$d/$s";done;__DIR__=$(cd -P "$(dirname "$s")" && pwd)
ROOT=$(cd "$__DIR__/.." && pwd)
FRAMEWORK_DIR="$ROOT/vendor/aklump/composer-repository-framework/"
